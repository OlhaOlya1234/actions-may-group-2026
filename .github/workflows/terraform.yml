name: Terraform CD

on:
  push:
    branches: [ "main" ]
  
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        name: Checkout repository
      
      - name: Set up Terraform
        uses: uses: hashicorp/setup-terraform@v4

      - name: Terraform Init
        run: terraform init